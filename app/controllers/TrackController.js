// const TrackDAO = require('../services/TrackDAO.js');

class TrackController {
  static getTrackById (req, res) {
    // TrackDAO.findById({ id: req.params.id })
    //         .then((track) => {
    //           res.status(200).json(track);
    //         });
  }

  static saveTrack (req, res) {
    // const { title, artist, preview } = req.body;
    // TrackDAO.createTrack({ title, artist, preview });
  }
}

