class Track {
  constructor ({ title, artist, preview, playlistID }) {
    this.title = title;
    this.artist = artist;
    this.preview = preview;
    this.playlistID = playlistID;
  }
}

module.exports = Track;
